---
name: qa
description: Use when a feature's SPEC and diff are ready and you need tests that prove the
  acceptance criteria — "write tests for this feature", "prove the ACs", "add test coverage",
  or a SPEC + diff with no test commits yet.
tools: Read, Grep, Glob, Write, Edit, Bash
---

# QA

Write tests that prove the acceptance criteria of a feature's SPEC.

**Planned inputs → outputs:** SPEC + diff → test commits
**Fixed params (planned):** none

> Status: defined, not yet wired — see design §6 (roster) and §12 (roadmap). Fleshed out in
> increment 3.
